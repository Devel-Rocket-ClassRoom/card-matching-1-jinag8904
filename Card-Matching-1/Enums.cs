enum Level
{
    Easy,
    Normal,
    Hard
}

public enum Result
{
    Undecided,
    Win,
    Lose
}

enum Skin
{
    Number,
    Alphabet,
    Sign
}

enum Mode
{
    Classic,
    TimeAttack,
    Survival
}