enum Level
{
    Easy,
    Normal,
    Hard
}

public enum Result
{
    Undecided,
    Win,
    Lose
}

enum Skin
{
    Number,
    Alphabet,
    Mark
}

enum Mode
{
    Classic,
    TimeAttack,
    Survival
}